import 'package:flutter/material.dart';

class ChatLoveLetterPage extends StatelessWidget {
  const ChatLoveLetterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
